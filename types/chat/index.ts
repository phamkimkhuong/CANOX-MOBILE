/**
 * Chat Types Index
 *
 * Re-exports all chat-related types for convenient imports.
 *
 * @example
 * import { Conversation, ChatFilter, ConversationDTO } from '@/types/chat';
 */

// UI Types (for components) - Conversation, LastMessage, ChatFilter, etc.
export * from './conversation';

// API DTO Types (for hooks/adapters) - Raw API response types
export * from './conversationDTO';

