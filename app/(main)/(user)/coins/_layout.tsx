/**
 * Coins Layout - Slot Layout cho loyalty screens
 * Using Slot per project convention to avoid Layout Hell
 */

import { Slot } from 'expo-router';

export default function CoinsLayout() {
    return <Slot />;
}
